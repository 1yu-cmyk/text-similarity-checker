# 演示视频脚本（约 5 分钟）

> 照着下面的分镜录屏 + 口播即可。每段括号里是参考台词，不用逐字背，讲清楚即可。

---

## 第 1 段 · 开场（0:00 – 0:30）

**画面**：桌面 / 项目文件夹
**口播**：
> 各位老师好，我的专业综合实训项目是《基于多算法融合的中文文本相似度分析与查重系统》。
> 这是一个用 Python 和 Streamlit 开发的轻量化文本查重工具，纯离线运行，不需要 GPU，也不需要联网。

---

## 第 2 段 · 启动系统（0:30 – 1:00）

**画面**：打开终端，进入项目目录，执行 `streamlit run app.py`，浏览器自动弹出界面
**口播**：
> 首先在终端运行 `streamlit run app.py` 启动系统，浏览器会自动打开操作界面。
> 整个界面分为"两篇文本比对"和"批量查重"两个标签页。

---

## 第 3 段 · 两篇文本比对（1:00 – 3:00）

**画面**：在"文本 A""文本 B"两栏分别粘贴两段文字（可用 data 里的 sample1、sample2），点击"开始比对"
**口播**：
> 在第一个标签页，我可以输入或上传两段文本。这里我粘贴两段内容相似的中文文字，
> 点击"开始比对"。系统会综合 TF-IDF 余弦、Jaccard、编辑距离、SimHash 四种算法，
> 加权融合后给出综合相似度和判定结论，下面是四项算法各自的得分。

**要点**：强调"综合得分 + 四种算法得分 + 判定结论"三个结果。

---

## 第 4 段 · 批量查重（3:00 – 4:30）

**画面**：切到"批量查重"标签页，上传多篇文档，点击"开始批量查重"
**口播**：
> 第二个标签页支持批量查重。我上传多篇文档，系统会两两比对，生成相似度矩阵、
> 热力图，并自动找出最相似的一对文档。从热力图可以直观看出哪些文档之间存在重复。

**要点**：指出"最相似文档对 + 矩阵 + 热力图"。

---

## 第 5 段 · 总结（4:30 – 5:00）

**画面**：回到首页 / 展示代码目录
**口播**：
> 总结一下，本系统用四种经典相似度算法加权融合，实现了两篇比对和批量查重两大功能，
> 轻量、离线、易部署。谢谢老师，我的演示到此结束。

---

## 录制小贴士

1. 提前准备两段**相似**文本（用 `data/sample1.txt` 和 `sample2.txt`）和几篇文档做批量查重。
2. 用 Windows 自带的 Xbox Game Bar（快捷键 `Win + G`）或 OBS 录屏。
3. 全程口播 + 录屏，声音清楚即可，不必加字幕。
4. 录完上传百度网盘 / 阿里云盘，把分享链接填进报告附录 B。
